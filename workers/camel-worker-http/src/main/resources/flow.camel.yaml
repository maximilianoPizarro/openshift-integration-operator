- route:
    id: placeholder
    from:
      uri: "timer:placeholder?period=999999999"
      steps:
        - log:
            message: "Ephemeral worker started — routes loaded from /deployments/config"
